scss_files: '_sass/**/*.scss'

linters:

  PropertySortOrder:
    order: concentric

  SelectorFormat:
    convention: hyphenated_BEM

  PropertySpelling:
    extra_properties:
      - cx
      - cy

  PropertyUnits:
    global: ['rem', '%', 's']
    properties:
      border: ['px', 'rem']
